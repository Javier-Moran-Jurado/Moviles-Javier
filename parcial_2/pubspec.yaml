name: parcial_2
description: "Aplicación Flutter para estadísticas de accidentes y CRUD de establecimientos."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  
  
  go_router: ^17.2.2
  
  
  dio: ^5.4.0
  
  
  flutter_dotenv: ^5.1.0
  
  
  fl_chart: ^1.2.0
  
  
  
 
  
  
  image_picker: ^1.0.7
  skeletonizer: ^2.1.3
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  
  assets:
    - .env